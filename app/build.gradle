plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("kotlin-kapt") // Necesario para Data Binding y Hilt si lo usas
}

android {
    namespace = "com.dronescan.msdksample"
    compileSdk = 34 // Asegúrate de que esta versión sea compatible con tus dependencias

    defaultConfig {
        applicationId = "com.dronescan.msdksample"
        minSdk = 24 // DJI SDK v5 requiere al menos API 24
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"

        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
        vectorDrawables {
            useSupportLibrary = true
        }
    }

    buildTypes {
        release {
            //isMinifyEnabled = false
            proguardFiles(
                    getDefaultProguardFile("proguard-android-optimize.txt"),
                    "proguard-rules.pro"
            )
        }
    }
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }
    kotlinOptions {
        jvmTarget = "1.8"
    }
    buildFeatures {
        compose = true
        dataBinding = true // Habilita Data Binding
    }
    composeOptions {
        kotlinCompilerExtensionVersion = "1.5.1" // Asegúrate de que esta versión sea compatible con tu versión de Kotlin
    }
    packaging {
        resources {
            excludes += "/META-INF/{AL2.0,LGPL2.1}"
        }
    }
}

dependencies {
    // Dependencias de AndroidX y UI
    implementation("androidx.core:core-ktx:1.13.1")
    implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.8.3")
    implementation("androidx.activity:activity-compose:1.9.0")
    implementation(platform("androidx.compose:compose-bom:2024.04.00"))
    implementation("androidx.compose.ui:ui")
    implementation("androidx.compose.ui:ui-graphics")
    implementation("androidx.compose.ui:ui-tooling-preview")
    implementation("androidx.compose.material3:material3")

    // *** Importante: Asegúrate de que appcompat esté incluido ***
    implementation("androidx.appcompat:appcompat:1.7.0") // Versión estable y reciente

    // Dependencias del DJI SDK v5 (versiones 5.15.0 según tu log)
    // Asegúrate de que estas versiones coincidan con las que estás tratando de usar
    //implementation("com.dji:dji-sdk-v5-common:5.15.0")
    //implementation("com.dji:dji-sdk-v5-component:5.15.0")
    //implementation("com.dji:dji-sdk-v5-keyvalue:5.15.0")
    //implementation("com.dji:dji-sdk-v5-manager:5.15.0")
    implementation("com.dji:dji-sdk-v5-aircraft:5.15.0") // Esta fue la que causó las advertencias de recursos

    // Si estás usando el UX SDK, asegúrate de tenerlo también
    // implementation("com.dji:dji-sdk-v5-uxsdk:5.15.0") // Descomenta si lo necesitas

    // Dependencias de prueba
    testImplementation("junit:junit:4.13.2")
    androidTestImplementation("androidx.test.ext:junit:1.2.1")
    androidTestImplementation("androidx.test.espresso:espresso-core:3.6.1")
    androidTestImplementation(platform("androidx.compose:compose-bom:2024.04.00"))
    androidTestImplementation("androidx.compose.ui:ui-test-junit4")
    debugImplementation("androidx.compose.ui:ui-tooling")
    debugImplementation("androidx.compose.ui:ui-test-manifest")
}
