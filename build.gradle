// build.gradle.kts (en la raíz de tu proyecto DroneScan_V3)

plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.kotlin.android) apply false
}

buildscript {
    ext {
        // Define las versiones aquí para usarlas en los módulos
        set("kotlin_version", "1.9.0")
        set("agp_version", "8.1.4")
        set("dji_sdk_version", "5.15.0")
        set("androidxCoreKtx", "1.9.0")
        set("androidxLifecycleRuntimeKtx", "2.6.2")
        set("androidxActivityCompose", "1.8.2")
        set("androidxComposeBom", "2023.08.00")
        set("androidxUi", "1.5.4")
        set("androidxUiGraphics", "1.5.4")
        set("androidxUiTooling", "1.5.4")
        set("androidxUiToolingPreview", "1.5.4")
        set("androidxMaterial3", "1.1.2")
        set("junit", "4.13.2")
        set("androidxJunit", "1.1.5")
        set("androidxEspressoCore", "3.5.1")
        set("androidxUiTestJunit4", "1.5.4")
        set("zxingCore", "3.5.3")
        set("zxingAndroidEmbeddedVersion", "4.3.0")
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:${rootProject.extra["agp_version"]}")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:${rootProject.extra["kotlin_version"]}")
    }
}

// ELIMINADO: El bloque allprojects para repositorios, ya que settings.gradle.kts lo maneja.

tasks.register("clean", Delete::class) {
    delete(layout.buildDirectory.get().asFile)
}
